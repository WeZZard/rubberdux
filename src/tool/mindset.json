{
  "type": "function",
  "function": {
    "name": "mindset",
    "description": "Manage agent mindset: responsibilities (ongoing areas of work the agent is accountable for).",
    "parameters": {
      "type": "object",
      "properties": {
        "entity": {
          "type": "string",
          "description": "The type of mindset entity to operate on.",
          "enum": ["responsibility"]
        },
        "action": {
          "type": "string",
          "description": "The CRUD action to perform.",
          "enum": ["list", "create", "retrieve", "update", "deactivate"]
        },
        "name": {
          "type": "string",
          "description": "Name/identifier of the entity. Required for create, retrieve, update, deactivate."
        },
        "fields": {
          "type": "object",
          "description": "Key-value pairs to set on the entity. Used with create and update. Responsibility fields: description.",
          "additionalProperties": { "type": "string" }
        }
      },
      "required": ["entity", "action"]
    }
  }
}
