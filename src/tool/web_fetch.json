{
  "type": "function",
  "function": {
    "name": "web_fetch",
    "description": "Fetch the content of a web page URL and return it as markdown. Uses a headless browser to render JavaScript-heavy pages. Always runs in the background.",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The URL to fetch (must be a fully-formed URL with http:// or https://)"
        }
      },
      "required": ["url"]
    }
  }
}
