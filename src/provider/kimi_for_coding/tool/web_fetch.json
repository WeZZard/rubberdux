{
  "type": "function",
  "function": {
    "name": "web_fetch",
    "description": "Fetch the content of a web page URL and return it as markdown. Supports JavaScript rendering for dynamic pages via Chrome/Chromium. Automatically falls back to static fetch if JS rendering is unavailable.",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The URL to fetch"
        }
      },
      "required": ["url"]
    }
  }
}