{
  "type": "function",
  "function": {
    "name": "web_fetch",
    "description": "Fetch the content of a web page URL and return it as markdown. Uses a headless browser to render JavaScript-heavy pages. This tool runs in the background — the result will be delivered automatically when ready. Respond to the user acknowledging the request is being processed.",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The URL to fetch (must be a fully-formed URL with http:// or https://)"
        }
      },
      "required": ["url"]
    }
  }
}
